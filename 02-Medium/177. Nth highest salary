CREATE FUNCTION getNthHighestSalary(@N INT) RETURNS INT AS
BEGIN
    RETURN (
        /* Write your T-SQL query statement below. */
       select max(e.salary) from
        (select salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS Rank from Employee) e
        where e.Rank = @N
    );
END
